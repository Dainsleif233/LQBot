{
  "name": "LQBot",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless QQ official bot on EdgeOne edge functions.",
  "scripts": {
    "dev": "edgeone makers dev -n LQBot",
    "deploy": "edgeone makers deploy -n LQBot",
    "register": "npx tsx scripts/register.ts",
    "smoke": "npx tsx scripts/smoke.ts",
    "test": "npm run dev",
    "start": "npm run register && npm run deploy"
  },
  "devDependencies": {
    "@types/node": "^26.5.0"
  }
}
